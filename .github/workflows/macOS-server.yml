name: macOS 桌面 + mefrp 穿透（VNC强制激活版）
on:
  workflow_dispatch:

jobs:
  macos-vnc-force-activate:
    runs-on: macos-latest
    timeout-minutes: 3600
    env:
      NEW_USER: macuser
      NEW_PASS: Mac@1234
      FRP_BIN: mefrpc_arm64
      FRP_TOKEN: bab042f57c6e615bc8692773cf2386dc
      FRP_SERVER_PORT: 125545
      VNC_PORT: 5900

    steps:
      # 1. 克隆仓库
      - name: 克隆仓库
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # 2. 创建/复用用户
      - name: 创建/复用用户
        run: |
          if ! dscl . -read /Users/$NEW_USER &> /dev/null; then
            sudo dscl . -create /Users/$NEW_USER
            sudo dscl . -create /Users/$NEW_USER UserShell /bin/bash
            sudo dscl . -create /Users/$NEW_USER UniqueID 1001
            sudo dscl . -create /Users/$NEW_USER PrimaryGroupID 20
            sudo dscl . -create /Users/$NEW_USER NFSHomeDirectory /Users/$NEW_USER
            sudo dscl . -passwd /Users/$NEW_USER $NEW_PASS
            sudo mkdir -p /Users/$NEW_USER
            sudo chown -R $NEW_USER:staff /Users/$NEW_USER
            sudo dseditgroup -o edit -a $NEW_USER -t user admin
            echo "✅ 用户创建成功"
          else
            echo "ℹ️ 用户已存在，直接使用"
          fi

      # 3. 显示配置（确保渲染基础）
      - name: 原生显示配置
        run: |
          sudo defaults write /Library/Preferences/com.apple.CoreDisplay enableHiDPI -bool true
          sudo defaults write /Library/Preferences/com.apple.windowserver.plist DisplayResolutionEnabled -bool true
          sudo defaults write /Library/Preferences/com.apple.windowserver.plist DisplayResolutionWidth -int 1024
          sudo defaults write /Library/Preferences/com.apple.windowserver.plist DisplayResolutionHeight -int 768
          sleep 30
          echo "✅ 显示配置完成"

      # 4. 核心中的核心：强制激活VNC（绕开系统权限锁定）
      - name: 强制激活VNC+会话绑定
        run: |
          # 1. 自动登录+确认会话创建（必须有用户会话）
          sudo defaults write /Library/Preferences/com.apple.loginwindow autoLoginUser $NEW_USER
          sudo open -a loginwindow
          sleep 60
          # 验证用户会话是否存在（关键前提）
          if pgrep -u $NEW_USER loginwindow; then
            echo "✅ 用户会话已创建"
          else
            echo "⚠️ 会话未创建，重试登录"
            sudo open -a loginwindow
            sleep 30
          fi

          # 2. 用ARD Agent强制激活屏幕共享（全权限，绕开系统锁定）
          sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart \
            -activate \
            -configure -access -on -users $NEW_USER -privs -all \
            -configure -allowAccessFor -allUsers \
            -configure -clientopts -setvnclegacy -vnclegacy yes \
            -configure -clientopts -setvncpw -vncpw $NEW_PASS \
            -restart -agent -menu
          sleep 20

          # 3. 直接启动VNC服务二进制文件（绕开launchctl限制）
          sudo /System/Library/CoreServices/Screen Sharing.app/Contents/MacOS/Screen Sharing &
          sleep 15

          # 4. 强制绑定VNC到5900端口（直接指定端口监听）
          sudo launchctl kickstart -k system/com.apple.screensharing
          sleep 10

          # 5. 多次重试端口监听（不轻易退出，给系统足够时间）
          RETRY=3
          while [ $RETRY -gt 0 ]; do
            if lsof -i :$VNC_PORT; then
              echo "✅ VNC端口 $VNC_PORT 已监听，服务稳定"
              exit 0
            else
              echo "⚠️ 第 $((4-RETRY)) 次重试激活VNC"
              sudo launchctl unload /System/Library/LaunchDaemons/com.apple.screensharing.plist
              sudo launchctl load -w /System/Library/LaunchDaemons/com.apple.screensharing.plist
              sudo /System/Library/CoreServices/Screen Sharing.app/Contents/MacOS/Screen Sharing &
              sleep 15
              RETRY=$((RETRY-1))
            fi
          done

          # 最后验证，仍失败则输出调试信息
          if lsof -i :$VNC_PORT; then
            echo "✅ 最终重试成功"
          else
            echo "❌ 端口仍未监听，输出服务状态"
            sudo launchctl list | grep screensharing
            sudo cat /var/log/system.log | grep -i screensharing | tail -20
            exit 1
          fi

      # 5. 关闭防火墙+禁用节能
      - name: 关闭防火墙+禁用节能
        run: |
          sudo /usr/libexec/ApplicationFirewall/socketfilterfw --setglobalstate off
          sudo pmset -a displaysleep 0
          sudo pmset -a sleep 0
          echo "✅ 防火墙关闭，节能禁用"

      # 6. 启动frp
      - name: 启动 mefrp 穿透
        run: |
          FRP_PATH="$GITHUB_WORKSPACE/.github/workflows/scripts/$FRP_BIN"
          chmod +x "$FRP_PATH"
          nohup "$FRP_PATH" -t "$FRP_TOKEN" -p "$FRP_SERVER_PORT" > ~/frp.log 2>&1 &
          sleep 5
          pgrep "$FRP_BIN" && echo "✅ frp启动成功" || (cat ~/frp.log && exit 1)

      # 7. 保活：VNC进程+端口双监测
      - name: 保活（双监测）
        run: |
          FRP_PATH="$GITHUB_WORKSPACE/.github/workflows/scripts/$FRP_BIN"
          while true; do
            # 监测进程+端口
            pgrep "Screen Sharing" || sudo /System/Library/CoreServices/Screen Sharing.app/Contents/MacOS/Screen Sharing &
            if ! lsof -i :$VNC_PORT; then
              echo "⚠️ VNC端口断开，重启服务"
              sudo launchctl kickstart -k system/com.apple.screensharing
            fi
            pgrep loginwindow || sudo open -a loginwindow
            pgrep "$FRP_B
            pgrep "$FRP_BIN" || nohup "$FRP_PATH" -t "$FRP_TOKEN" -p "$FRP_SERVER_PORT" > ~/frp.log 2>&1 &

            echo "[$(date)] 运行中 → VNC端口：$VNC_PORT（监听中） | 穿透：你的frp服务器:$FRP_SERVER_PORT"
            sleep 60
          done
