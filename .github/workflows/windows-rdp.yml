name: Windows RDP Server mefrp

on:
  workflow_dispatch:

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 3600

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure Core RDP Settings
        shell: pwsh
        run: .github/workflows/scripts/win-configure-rdp.ps1

      - name: Create RDP User with Secure Password
        shell: pwsh
        run: .github/workflows/scripts/win-create-user.ps1

      - name: Install frp (沿用原代码逻辑，解决卡住问题)
        shell: pwsh
        run: |
          # 完全沿用你原有的直接运行方式，只加一行“后台运行”不阻塞步骤
          Start-Job -ScriptBlock {
            .github/workflows/scripts/mefrpc.exe -t bab042f57c6e615bc8692773cf2386dc -p 50000 > $null 2>&1
          }
          Start-Sleep -Seconds 5  # 等待客户端启动连接

      - name: Maintain Connection
        shell: pwsh
        run: |
          Write-Host "`n=== RDP ACCESS ==="
          Write-Host "Address: 156.231.141.29:33890"
          Write-Host "Username: vum"
          Write-Host "Password: $(echo $env:RDP_CREDS)"
          Write-Host "==================`n"
          while ($true) {
            # 检查 frp 进程，确保连接不中断
            $frpProcess = Get-Process "mefrpc" -ErrorAction SilentlyContinue
            if (-not $frpProcess) {
              Write-Host "[$(Get-Date)] frp 进程已退出，重启中..."
              Start-Job -ScriptBlock {
                .github/workflows/scripts/mefrpc.exe -t bab042f57c6e615bc8692773cf2386dc -p 50000 > $null 2>&1
              }
            }
            Write-Host "[$(Get-Date)] RDP Active - Use Ctrl+C in workflow to terminate"
            Start-Sleep -Seconds 300
          }
